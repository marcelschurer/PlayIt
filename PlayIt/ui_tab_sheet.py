import tkinter as tk

from ui_base import UIBase

class UITabSheet(UIBase):
    # PLACEHOLDER CLASS NOT FINISHED
    def __init__(self):
        super().__init__()
        ################ Implementing PLACEHOLDER Rythm frame for tabs and sheet ##################################################################
        new_label = tk.Label(self.frame_rthm, text=f"!!! NOT YET IMPLEMENTED !!!", font=("bold"), fg="red", bg="lightgreen")
        new_label.grid(row=0, column=0, padx=1, pady=0, sticky="sw")